#pragma once

struct BoidsParams {
    float turnFactor;
    float visualRange;
    float protectedRange;
    float centeringFactor;
    float avoidFactor;
    float matchingFactor;
    float maxSpeed;
    float minSpeed;
};